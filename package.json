{
  "name": "conference-website",
  "version": "1.0.0",
  "description": "Conference website with React frontend and Node.js backend",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
